== README

* Database creation
	<tt>rake db:create db:migrate</tt>

* Database initialization
After creating and migrating database, seed database with (1 million records, should take around 10 mins): 
	<tt>rake db:seed</tt>.

* Run tests
	<tt>rake test</tt>.
